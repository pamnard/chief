# Локальные инструкции для агента Cursor — не коммитим
AGENTS.md
.cursor/rules/

# Локальная документация — не коммитим
docs/

# На случай файлов в корне — тоже не в git (дубли из docs/)
RELEASING.md
SPEC.md

# Локальный chief.toml в корне репо (если кто-то кладёт конфиг сюда вместо XDG)
config/chief.toml

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
dist/
build/
.pytest_cache/
